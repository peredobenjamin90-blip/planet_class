# 🌍 Planet Class – OOP Python Project

This project models planets using a Python class.  
Each planet has a `name`, `planet_type`, and `star`, with validation to ensure all values are non-empty strings.  
The class includes:

### 🔸 Features
- `__init__()` validates input and creates planet objects
- `__str__()` returns a formatted summary when printed
- `orbit()` returns a sentence describing the planet orbiting its star

### 🪐 Example Output
